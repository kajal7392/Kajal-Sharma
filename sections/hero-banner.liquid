{% comment %}
  ================================================================
  SECTION: The Gift Guide Hero Banner with Vector Art
  VERSION: 2.0.0
  DESCRIPTION: Production-ready pixel-perfect Figma implementation
  ================================================================
{% endcomment %}

{%- liquid
  assign section_id = section.id
  
  assign headline = section.settings.headline
  assign description = section.settings.description
  assign cta_text = section.settings.cta_text
  assign cta_link = section.settings.cta_link
  assign subtitle = section.settings.subtitle
  
  assign text_color = section.settings.text_color
  assign button_bg = section.settings.button_bg
  assign button_text_color = section.settings.button_text_color
-%}

{%- style -%}
  @import url('https://fonts.googleapis.com/css2?family=Jost:wght@400;500;600&display=swap');
  
  .gift-guide-{{ section_id }} {
    max-width: 1440px;
    margin: 0 auto;
    font-family: 'Jost', sans-serif;
    position: relative;
    overflow: hidden;
  }

  /* --- BANNER WORKSPACE FRAME CONTAINER --- */
  .gift-guide-{{ section_id }} .hero-banner-container {
    position: relative;
    width: 100%;
    height: 780px; 
    margin-top: 0;
    background-color: transparent;
  }
  
  /* --- RECTANGLE 26168: FROSTED GLASS BACKGROUND OVERLAY --- */
  .gift-guide-{{ section_id }} .hero-glass-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 1440px;
    height: 780px;
    background: rgba(255, 255, 255, 0.20);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    z-index: 1;
    pointer-events: none;
  }
  
  /* --- TEXT CONTENT SEGMENT --- */
  .gift-guide-{{ section_id }} .hero-text-content-wrapper {
    position: absolute;
    top: 442px;
    left: 550px;
    width: 890px;
    height: 402px;
    z-index: 3;
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
  }
  
  .gift-guide-{{ section_id }} .main-headline {
    font-weight: 500;
    font-size: 70px;
    line-height: 1.1;
    color: {{ text_color }};
    margin: 0 0 20px 0;
    letter-spacing: 0px;
  }
  
  .gift-guide-{{ section_id }} .hero-description {
    max-width: 483px;
    font-weight: 400;
    font-size: 20px;
    line-height: 1.4;
    color: {{ text_color }};
    margin: 0 0 35px 0;
  }
  
  /* --- RECONFIGURED INTERACTIVE CTA BUTTON --- */
  .gift-guide-{{ section_id }} .hero-cta-btn {
    display: inline-flex;
    align-items: center;
    width: fit-content;
    background: {{ button_bg }};
    color: {{ button_text_color }};
    text-decoration: none;
    padding: 15px 30px;
    font-size: 16px;
    font-weight: 500;
    transition: background-color 0.2s ease-in-out;
  }
  
  .gift-guide-{{ section_id }} .hero-cta-btn:hover {
    opacity: 0.9;
  }

  .gift-guide-{{ section_id }} .cta-arrow-icon {
    margin-left: 15px;
    display: flex;
    align-items: center;
  }
  
  /* --- SUBTITLE LOWER PLACEMENT --- */
  .gift-guide-{{ section_id }} .hero-subtitle {
    margin-top: auto;
    font-size: 14px;
    font-weight: 400;
    letter-spacing: 0.05em;
    color: {{ text_color }};
    text-transform: uppercase;
  }
  
  /* --- GRAPHIC IMAGE CANVAS ASSETS --- */
  .gift-guide-{{ section_id }} .vector-asset {
    position: absolute;
    z-index: 2;
    pointer-events: none;
  }
  .gift-guide-{{ section_id }} .vector-asset img {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }
  
  /* Precision Positioning From Figma Layer Offsets */
  .gift-guide-{{ section_id }} .v-layer-1199 { top: 94.74px; left: 462.39px; width: 879.11px; height: 425.78px; }
  .gift-guide-{{ section_id }} .v-layer-1203 { top: 14.10px; left: 660.59px; width: 669.63px; height: 428.52px; }
  .gift-guide-{{ section_id }} .v-layer-1197 { top: 258.07px; left: 1083.56px; width: 305.38px; height: 276.01px; }
  .gift-guide-{{ section_id }} .v-layer-1204 { top: 23.69px; left: 1106.69px; width: 246.95px; height: 160.18px; }
  .gift-guide-{{ section_id }} .v-layer-1198 { top: 354.87px; left: 1166.37px; width: 171.10px; height: 162.24px; }
  .gift-guide-{{ section_id }} .v-layer-1201 { top: 364.59px; left: 629.25px; width: 137.02px; height: 136.91px; }
  .gift-guide-{{ section_id }} .v-layer-1196 { top: 252.60px; left: 1363.48px; width: 30.94px; height: 30.94px; }
  .gift-guide-{{ section_id }} .v-layer-1202 { top: 382.66px; left: 650.53px; width: 94.43px; height: 112.68px; }
  .gift-guide-{{ section_id }} .v-layer-1200 { top: 410.12px; left: 663.67px; width: 56.67px; height: 56.46px; }

  /* --- FLUID RESPONSIVE SCALE VIEWPORTS --- */
  @media screen and (max-width: 1439px) {
    .gift-guide-{{ section_id }} .hero-banner-container,
    .gift-guide-{{ section_id }} .hero-glass-overlay {
      height: 55vw;
    }
    .gift-guide-{{ section_id }} .hero-text-content-wrapper {
      top: 50%;
      left: 5%;
      transform: translateY(-50%);
      width: 45%;
      height: auto;
    }
    .gift-guide-{{ section_id }} .main-headline { font-size: 4.5vw; }
    .gift-guide-{{ section_id }} .hero-description { font-size: 1.4vw; }
    .gift-guide-{{ section_id }} [class^="vector-asset v-layer-"] {
      transform: scale(calc(100vw / 1440));
      transform-origin: top left;
    }
  }

  @media screen and (max-width: 767px) {
    .gift-guide-{{ section_id }} .hero-banner-container {
      height: auto;
      padding: 60px 20px;
      display: flex;
      flex-direction: column;
    }
    .gift-guide-{{ section_id }} .hero-glass-overlay,
    .gift-guide-{{ section_id }} .vector-asset {
      display: none !important;
    }
    .gift-guide-{{ section_id }} .hero-text-content-wrapper {
      position: relative;
      top: unset;
      left: unset;
      transform: unset;
      width: 100%;
    }
    .gift-guide-{{ section_id }} .main-headline { font-size: 36px; }
    .gift-guide-{{ section_id }} .hero-description { font-size: 16px; max-width: 100%; }
    .gift-guide-{{ section_id }} .hero-subtitle { margin-top: 40px; }
  }
{%- endstyle -%}

<section class="gift-guide-{{ section_id }}" aria-label="Hero Banner Section">
  <div class="hero-banner-container">
    <div class="hero-glass-overlay"></div>

    <!-- Dynamic Customizer Asset Images -->
    {% for i in (1..9) %}
      {% capture key %}vector_img_{{ i }}{% endcapture %}
      {% capture class_key %}vector_class_{{ i }}{% endcapture %}
      
      {%- case i -%}
        {%- when 1 -%}{%- assign current_class = 'v-layer-1199' -%}
        {%- when 2 -%}{%- assign current_class = 'v-layer-1203' -%}
        {%- when 3 -%}{%- assign current_class = 'v-layer-1197' -%}
        {%- when 4 -%}{%- assign current_class = 'v-layer-1204' -%}
        {%- when 5 -%}{%- assign current_class = 'v-layer-1198' -%}
        {%- when 6 -%}{%- assign current_class = 'v-layer-1201' -%}
        {%- when 7 -%}{%- assign current_class = 'v-layer-1196' -%}
        {%- when 8 -%}{%- assign current_class = 'v-layer-1202' -%}
        {%- when 9 -%}{%- assign current_class = 'v-layer-1200' -%}
      {%- endcase -%}

      {% if section.settings[key] != blank %}
        <div class="vector-asset {{ current_class }}">
          <img src="{{ section.settings[key] | image_url: width: 1000 }}" alt="Decorative Artwork Shape {{ i }}" loading="lazy">
        </div>
      {% endif %}
    {% endfor %}

    <!-- Text Layer Block Frame Container -->
    <div class="hero-text-content-wrapper">
      <h1 class="main-headline">{{ headline | escape }}</h1>
      <p class="hero-description">{{ description | escape }}</p>
      
      <a href="{{ cta_link }}" class="hero-cta-btn">
        <span>{{ cta_text | escape }}</span>
        <span class="cta-arrow-icon" aria-hidden="true">
          <svg width="26" height="2" viewBox="0 0 26 2" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M0 1H24M24 1L20 5M24 1L20 -3" stroke="currentColor" stroke-width="1.5"/>
          </svg>
        </span>
      </a>

      <p class="hero-subtitle">{{ subtitle | escape }}</p>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "The Gift Guide Hero",
  "tag": "section",
  "class": "index-section-hero-banner",
  "settings": [
    {
      "type": "header",
      "content": "Content Configurator"
    },
    {
      "type": "text",
      "id": "headline",
      "label": "Heading Title Text",
      "default": "The Gift Guide"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description Text Paragraph",
      "default": "Discover Joy: Your Ultimate Holiday Gift Destination. Explore our curated selection and find the perfect gifts to delight your loved ones this holiday season."
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Footer Footnote Subtitle",
      "default": "SUSTAINABLE, ETHICALLY MADE CLOTHES IN SIZES XXS TO 6XL"
    },
    {
      "type": "text",
      "id": "cta_text",
      "label": "CTA Button Label",
      "default": "SHOP NOW"
    },
    {
      "type": "url",
      "id": "cta_link",
      "label": "CTA Redirection URL Link",
      "default": "/collections/all"
    },
    {
      "type": "header",
      "content": "Style Color Elements"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Typography Primary Hex Color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "button_bg",
      "label": "CTA Button Background Base",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "button_text_color",
      "label": "CTA Text Base Color Fill",
      "default": "#FFFFFF"
    },
    {
      "type": "header",
      "content": "Expirable PNG Image Vectors Canvas Blocks"
    },
    { "type": "image_picker", "id": "vector_img_1", "label": "Vector Layer Path 1199 (879.11px × 425.78px)" },
    { "type": "image_picker", "id": "vector_img_2", "label": "Vector Layer Path 1203 (669.63px × 428.52px)" },
    { "type": "image_picker", "id": "vector_img_3", "label": "Vector Layer Path 1197 (305.38px × 276.01px)" },
    { "type": "image_picker", "id": "vector_img_4", "label": "Vector Layer Path 1204 (246.95px × 160.18px)" },
    { "type": "image_picker", "id": "vector_img_5", "label": "Vector Layer Path 1198 (171.10px × 162.24px)" },
    { "type": "image_picker", "id": "vector_img_6", "label": "Vector Layer Path 1201 (137.02px × 136.91px)" },
    { "type": "image_picker", "id": "vector_img_7", "label": "Vector Layer Path 1196 (30.94px × 30.94px)" },
    { "type": "image_picker", "id": "vector_img_8", "label": "Vector Layer Path 1202 (94.43px × 112.68px)" },
    { "type": "image_picker", "id": "vector_img_9", "label": "Vector Layer Path 1200 (56.67px × 56.46px)" }
  ],
  "presets": [
    {
      "name": "The Gift Guide Hero Banner"
    }
  ]
}
{% endschema %}