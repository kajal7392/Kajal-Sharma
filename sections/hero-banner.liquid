{% comment %}
  ================================================================
  SECTION: The Gift Guide Hero Banner (Clean Version)
  VERSION: 1.0.0
  DESCRIPTION: Clean, minimal hero section with background image
               support, perfect for The Gift Guide campaign
  AUTHOR: [Candidate Name]
  
  FEATURES:
  - Upload background banner image via Theme Editor
  - Frosted glass overlay effect
  - Full typography control with Jost font
  - Responsive on all devices
  - Clean, minimal design without vector clutter
  - All Figma-compliant positioning preserved
  
  DIMENSIONS:
  - Frame 1957: 1440px × 878px
  - Header: 1440px × 64px
  - Background Canvas: 1440px × 780px
  - Frosted Glass Overlay: 1440px × 780px
  ================================================================
{% endcomment %}

{%- liquid
  # ============================================================
  # SECTION VARIABLES
  # ============================================================
  assign section_id = section.id
  
  # Header settings
  assign header_bg = section.settings.header_bg | default: '#F5F5F5'
  
  # Background settings
  assign bg_image = section.settings.bg_image
  assign bg_color = section.settings.bg_color | default: '#FFFFFF'
  assign overlay_color = section.settings.overlay_color | default: '#FFFFFF'
  assign overlay_opacity = section.settings.overlay_opacity | default: 15
  
  # Hero content settings
  assign headline = section.settings.headline | default: 'The Gift Guide'
  assign description = section.settings.description | default: 'Discover Joy: Your Ultimate Holiday Gift Destination. Explore our curated selection and find the perfect gifts to delight your loved ones this holiday season.'
  assign cta_text = section.settings.cta_text | default: 'SHOP NOW'
  assign cta_link = section.settings.cta_link | default: '/collections/all'
  assign subtitle = section.settings.subtitle | default: 'SUSTAINABLE, ETHICALLY MADE CLOTHES IN SIZES XXS TO 6XL'
  
  # Color settings
  assign text_color = section.settings.text_color | default: '#000000'
  assign button_bg = section.settings.button_bg | default: '#000000'
  assign button_text_color = section.settings.button_text_color | default: '#FFFFFF'
  
  # Font settings
  assign font_family = section.settings.font_family | default: 'Jost'
-%}

<!-- ============================================================
     SECTION STYLES
     ============================================================ -->
{%- style -%}
  /* ============================================================
     FONT IMPORT (Jost)
     ============================================================ */
  @import url('https://fonts.googleapis.com/css2?family=Jost:wght@400;500;600;700&display=swap');
  
  /* ============================================================
     MASTER WRAPPER: Frame 1957
     Dimensions: 1440px × 878px
     ============================================================ */
  .gift-guide-{{ section_id }} {
    max-width: 1440px;
    margin: 0 auto;
    background: {{ header_bg }};
    font-family: '{{ font_family }}', sans-serif;
    position: relative;
    overflow: hidden;
  }
  
  /* ============================================================
     HEADER COMPONENT: 1440px × 64px
     ============================================================ */
  .gift-guide-{{ section_id }} .header-component {
    width: 100%;
    height: 64px;
    background: {{ header_bg }};
    display: flex;
    align-items: center;
    padding: 0 67px;
    position: relative;
    z-index: 10;
    border-bottom: 1px solid rgba(0,0,0,0.05);
  }
  
  /* ============================================================
     HEADER LOGO: 151.44px × 19px
     ============================================================ */
  .gift-guide-{{ section_id }} .header-logo {
    display: flex;
    align-items: center;
    width: 151.44px;
    height: 19px;
    position: relative;
  }
  
  .gift-guide-{{ section_id }} .logo-tisso {
    display: flex;
    align-items: center;
    height: 19px;
    position: relative;
  }
  
  .gift-guide-{{ section_id }} .logo-char {
    display: inline-block;
    background: {{ text_color }};
    position: absolute;
    top: 0;
  }
  
  /* "TISSO" letters - exact Figma positioning */
  .gift-guide-{{ section_id }} .char-t {
    width: 9.32px;
    height: 18.64px;
    left: 0px;
    top: 0px;
  }
  
  .gift-guide-{{ section_id }} .char-i1 {
    width: 3.11px;
    height: 18.64px;
    left: 12.32px;
    top: 0px;
  }
  
  .gift-guide-{{ section_id }} .char-s1 {
    width: 11.41px;
    height: 18.82px;
    left: 19.26px;
    top: 0px;
  }
  
  .gift-guide-{{ section_id }} .char-s2 {
    width: 11.41px;
    height: 18.82px;
    left: 32.87px;
    top: 0px;
  }
  
  .gift-guide-{{ section_id }} .char-o1 {
    width: 18.64px;
    height: 18.64px;
    left: 46.44px;
    top: 0px;
  }
  
  /* "VISON" letters - exact Figma positioning */
  .gift-guide-{{ section_id }} .logo-vison {
    display: flex;
    align-items: center;
    height: 19px;
    position: relative;
    margin-left: 0;
  }
  
  .gift-guide-{{ section_id }} .char-v {
    width: 16.15px;
    height: 19px;
    left: 0px;
    top: 0px;
  }
  
  .gift-guide-{{ section_id }} .char-i2 {
    width: 3.11px;
    height: 18.64px;
    left: 18.52px;
    top: 0px;
  }
  
  .gift-guide-{{ section_id }} .char-s3 {
    width: 11.41px;
    height: 18.82px;
    left: 24.69px;
    top: 0px;
  }
  
  .gift-guide-{{ section_id }} .char-o2 {
    width: 18.64px;
    height: 18.64px;
    left: 38.27px;
    top: 0px;
  }
  
  .gift-guide-{{ section_id }} .char-n {
    width: 15.53px;
    height: 19px;
    left: 59.26px;
    top: 0px;
  }
  
  /* ============================================================
     BACKGROUND CANVAS: Rectangle 26167
     Dimensions: 1440px × 780px, Top: 64px
     ============================================================ */
  .gift-guide-{{ section_id }} .background-canvas {
    position: absolute;
    top: 64px;
    left: 0;
    width: 100%;
    height: 780px;
    background: {{ bg_color }};
    z-index: 0;
    overflow: hidden;
  }
  
  /* Background image with parallax support */
  .gift-guide-{{ section_id }} .background-canvas .bg-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    display: block;
  }
  
  {%- if bg_image != blank -%}
    .gift-guide-{{ section_id }} .background-canvas {
      background-image: url('{{ bg_image | image_url: width: 1600 }}');
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
    }
  {%- endif -%}
  
  /* ============================================================
     FROSTED GLASS OVERLAY: Rectangle 26168
     Dimensions: 1440px × 780px, Top: 64px
     ============================================================ */
  .gift-guide-{{ section_id }} .glass-overlay {
    position: absolute;
    top: 64px;
    left: 0;
    width: 100%;
    height: 780px;
    background: {{ overlay_color }};
    opacity: {{ overlay_opacity | divided_by: 100.0 }};
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    z-index: 1;
    pointer-events: none;
  }
  
  /* ============================================================
     HERO CONTENT AREA: Frame 1956
     Dimensions: 1440px × 814px
     ============================================================ */
  .gift-guide-{{ section_id }} .hero-container {
    position: relative;
    min-height: 814px;
    padding: 0;
    z-index: 2;
  }
  
  /* ============================================================
     TEXT CONTENT - EXACT FIGMA POSITIONING
     ============================================================ */
  
  /* Main Headline: 459px × 70px at (67px, 489px) */
  .gift-guide-{{ section_id }} .main-headline {
    position: absolute;
    top: 489px;
    left: 67px;
    width: 459px;
    height: 70px;
    z-index: 3;
    font-family: '{{ font_family }}', sans-serif;
    font-weight: 500;
    font-size: 70px;
    line-height: 70px;
    color: {{ text_color }};
    margin: 0;
    padding: 0;
    letter-spacing: 0;
  }
  
  /* Description: 483px × 84px at (67px, 609px) */
  .gift-guide-{{ section_id }} .hero-description {
    position: absolute;
    top: 609px;
    left: 67px;
    width: 483px;
    height: 84px;
    z-index: 3;
    font-family: '{{ font_family }}', sans-serif;
    font-weight: 400;
    font-size: 20px;
    line-height: 140%;
    color: {{ text_color }};
    margin: 0;
    padding: 0;
    letter-spacing: 0;
  }
  
  /* Primary CTA: 220px × 50px at (67px, 739px) */
  .gift-guide-{{ section_id }} .primary-cta {
    position: absolute;
    top: 739px;
    left: 67px;
    width: 220px;
    height: 50px;
    z-index: 3;
    display: flex;
    align-items: center;
    justify-content: center;
    background: {{ button_bg }};
    color: {{ button_text_color }};
    text-decoration: none;
    font-family: '{{ font_family }}', sans-serif;
    font-weight: 400;
    font-size: 16px;
    line-height: 20.48px;
    letter-spacing: 0;
    transition: all 0.3s ease;
    cursor: pointer;
    border: none;
  }
  
  .gift-guide-{{ section_id }} .primary-cta .cta-content {
    display: flex;
    align-items: center;
    gap: 24px;
    padding: 0 16px;
  }
  
  .gift-guide-{{ section_id }} .primary-cta .cta-arrow {
    width: 25.51px;
    height: 0;
    border-top: 1.5px solid {{ button_text_color }};
    display: inline-block;
    position: relative;
  }
  
  .gift-guide-{{ section_id }} .primary-cta .cta-arrow::after {
    content: '';
    position: absolute;
    right: -4px;
    top: -4px;
    width: 8px;
    height: 8px;
    border-right: 1.5px solid {{ button_text_color }};
    border-top: 1.5px solid {{ button_text_color }};
    transform: rotate(45deg);
  }
  
  .gift-guide-{{ section_id }} .primary-cta:hover {
    opacity: 0.85;
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
  }
  
  /* Footnote Subtitle: 451px × 14px 
     Positioned at bottom: 44px, left: 67px */
  .gift-guide-{{ section_id }} .hero-subtitle {
    position: absolute;
    bottom: 44px;
    left: 67px;
    width: 451px;
    height: 14px;
    z-index: 3;
    font-family: '{{ font_family }}', sans-serif;
    font-weight: 400;
    font-size: 16px;
    line-height: 13.42px;
    color: {{ text_color }};
    margin: 0;
    padding: 0;
    letter-spacing: 0;
    opacity: 0.7;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  
  /* ============================================================
     RESPONSIVE ADAPTATIONS
     ============================================================ */
  
  @media screen and (max-width: 1024px) {
    .gift-guide-{{ section_id }} .header-component {
      padding: 0 30px;
    }
    
    .gift-guide-{{ section_id }} .main-headline {
      top: 350px;
      left: 40px;
      width: 350px;
      font-size: 50px;
      line-height: 50px;
    }
    
    .gift-guide-{{ section_id }} .hero-description {
      top: 430px;
      left: 40px;
      width: 350px;
      height: auto;
      font-size: 18px;
    }
    
    .gift-guide-{{ section_id }} .primary-cta {
      top: 560px;
      left: 40px;
    }
    
    .gift-guide-{{ section_id }} .hero-subtitle {
      bottom: 30px;
      left: 40px;
      width: 300px;
      font-size: 14px;
      white-space: normal;
    }
    
    .gift-guide-{{ section_id }} .background-canvas,
    .gift-guide-{{ section_id }} .glass-overlay {
      height: 100%;
      min-height: 780px;
    }
  }
  
  @media screen and (max-width: 749px) {
    .gift-guide-{{ section_id }} .header-component {
      padding: 0 20px;
      height: 56px;
    }
    
    .gift-guide-{{ section_id }} .header-logo {
      width: 120px;
      height: 16px;
      transform: scale(0.8);
      transform-origin: left center;
    }
    
    .gift-guide-{{ section_id }} .hero-container {
      min-height: 600px;
    }
    
    .gift-guide-{{ section_id }} .main-headline {
      top: 200px;
      left: 20px;
      width: calc(100% - 40px);
      height: auto;
      font-size: 32px;
      line-height: 36px;
      text-align: center;
    }
    
    .gift-guide-{{ section_id }} .hero-description {
      top: 260px;
      left: 20px;
      width: calc(100% - 40px);
      height: auto;
      font-size: 16px;
      text-align: center;
    }
    
    .gift-guide-{{ section_id }} .primary-cta {
      top: 420px;
      left: 50%;
      transform: translateX(-50%);
      width: 200px;
      height: 48px;
    }
    
    .gift-guide-{{ section_id }} .primary-cta:hover {
      transform: translateX(-50%) translateY(-2px);
    }
    
    .gift-guide-{{ section_id }} .hero-subtitle {
      bottom: 20px;
      left: 20px;
      width: calc(100% - 40px);
      height: auto;
      text-align: center;
      font-size: 12px;
      white-space: normal;
    }
    
    .gift-guide-{{ section_id }} .background-canvas,
    .gift-guide-{{ section_id }} .glass-overlay {
      height: 100%;
      min-height: 600px;
    }
  }
  
  @media screen and (max-width: 480px) {
    .gift-guide-{{ section_id }} .main-headline {
      font-size: 26px;
      line-height: 30px;
      top: 160px;
    }
    
    .gift-guide-{{ section_id }} .hero-description {
      font-size: 14px;
      top: 220px;
    }
    
    .gift-guide-{{ section_id }} .primary-cta {
      top: 380px;
      width: 180px;
      height: 44px;
      font-size: 14px;
    }
  }
{%- endstyle -%}

<!-- ============================================================
     SECTION HTML
     ============================================================ -->
<section 
  class="gift-guide-{{ section_id }}"
  id="gift-guide-{{ section_id }}"
  data-section-id="{{ section_id }}"
  data-section-type="gift-guide-hero"
  aria-label="The Gift Guide Hero Banner"
  role="banner"
>

  <!-- ============================================================
       HEADER: 1440px × 64px
       ============================================================ -->
  <header class="header-component" role="banner" aria-label="Site header">
    <div class="header-logo" aria-label="TISSO VISON logo">
      <!-- "TISSO" -->
      <span class="logo-tisso" aria-hidden="true">
        <span class="logo-char char-t"></span>
        <span class="logo-char char-i1"></span>
        <span class="logo-char char-s1"></span>
        <span class="logo-char char-s2"></span>
        <span class="logo-char char-o1"></span>
      </span>
      
      <!-- "VISON" -->
      <span class="logo-vison" aria-hidden="true">
        <span class="logo-char char-v"></span>
        <span class="logo-char char-i2"></span>
        <span class="logo-char char-s3"></span>
        <span class="logo-char char-o2"></span>
        <span class="logo-char char-n"></span>
      </span>
    </div>
  </header>

  <!-- ============================================================
       BACKGROUND CANVAS: Rectangle 26167
       Dimensions: 1440px × 780px, Top: 64px
       ============================================================ -->
  <div class="background-canvas" aria-hidden="true">
    {%- if bg_image != blank -%}
      <img 
        src="{{ bg_image | image_url: width: 1600 }}"
        srcset="
          {{ bg_image | image_url: width: 375 }} 375w,
          {{ bg_image | image_url: width: 750 }} 750w,
          {{ bg_image | image_url: width: 1100 }} 1100w,
          {{ bg_image | image_url: width: 1600 }} 1600w
        "
        sizes="(max-width: 749px) 100vw, 1600px"
        alt="Background banner image"
        class="bg-image"
        loading="lazy"
      >
    {%- endif -%}
  </div>

  <!-- ============================================================
       FROSTED GLASS OVERLAY: Rectangle 26168
       ============================================================ -->
  <div class="glass-overlay" aria-hidden="true"></div>

  <!-- ============================================================
       HERO CONTENT AREA: Frame 1956
       ============================================================ -->
  <div class="hero-container">
    
    <!-- ============================================================
         TEXT CONTENT - EXACT FIGMA POSITIONING
         ============================================================ -->
    
    <!-- Main Headline -->
    <h1 class="main-headline">
      {{ headline }}
    </h1>
    
    <!-- Description -->
    <p class="hero-description">
      {{ description }}
    </p>
    
    <!-- Primary CTA -->
    <a 
      href="{{ cta_link }}"
      class="primary-cta"
      aria-label="{{ cta_text | escape }}"
    >
      <span class="cta-content">
        {{ cta_text }}
        <span class="cta-arrow" aria-hidden="true"></span>
      </span>
    </a>
    
    <!-- Footnote Subtitle -->
    <p class="hero-subtitle">
      {{ subtitle }}
    </p>
    
  </div>
</section>

<!-- ============================================================
     SECTION SCHEMA
     ============================================================ -->
{% schema %}
{
  "name": "The Gift Guide Hero",
  "tag": "section",
  "class": "gift-guide-section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "header",
      "content": "🖼️ Background Settings"
    },
    {
      "type": "image_picker",
      "id": "bg_image",
      "label": "Background Banner Image",
      "info": "Recommended size: 1440px × 780px. The image will cover the entire banner area."
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color (Fallback)",
      "default": "#FFFFFF",
      "info": "Shown if no image is selected or while image loads"
    },
    {
      "type": "color",
      "id": "overlay_color",
      "label": "Overlay Color",
      "default": "#FFFFFF",
      "info": "Color for the frosted glass effect"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "label": "Overlay Opacity",
      "min": 0,
      "max": 100,
      "step": 5,
      "default": 15,
      "unit": "%",
      "info": "Higher values make the background less visible"
    },
    {
      "type": "header",
      "content": "📝 Content Settings"
    },
    {
      "type": "text",
      "id": "headline",
      "label": "Main Headline",
      "default": "The Gift Guide"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Discover Joy: Your Ultimate Holiday Gift Destination. Explore our curated selection and find the perfect gifts to delight your loved ones this holiday season."
    },
    {
      "type": "text",
      "id": "cta_text",
      "label": "CTA Button Text",
      "default": "SHOP NOW"
    },
    {
      "type": "url",
      "id": "cta_link",
      "label": "CTA Button Link",
      "default": "/collections/all"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Footnote Subtitle",
      "default": "SUSTAINABLE, ETHICALLY MADE CLOTHES IN SIZES XXS TO 6XL",
      "info": "Appears at the bottom of the banner (451px × 14px)"
    },
    {
      "type": "header",
      "content": "🎨 Colors"
    },
    {
      "type": "color",
      "id": "header_bg",
      "label": "Header Background",
      "default": "#F5F5F5"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "button_bg",
      "label": "Button Background",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "button_text_color",
      "label": "Button Text Color",
      "default": "#FFFFFF"
    },
    {
      "type": "header",
      "content": "📐 Typography"
    },
    {
      "type": "select",
      "id": "font_family",
      "label": "Font Family",
      "options": [
        { "value": "Jost", "label": "Jost" },
        { "value": "Inter", "label": "Inter" },
        { "value": "Helvetica Neue", "label": "Helvetica Neue" },
        { "value": "Arial", "label": "Arial" }
      ],
      "default": "Jost"
    }
  ],
  "presets": [
    {
      "name": "The Gift Guide Hero",
      "category": "Custom Sections",
      "settings": {
        "headline": "The Gift Guide",
        "description": "Discover Joy: Your Ultimate Holiday Gift Destination. Explore our curated selection and find the perfect gifts to delight your loved ones this holiday season.",
        "subtitle": "SUSTAINABLE, ETHICALLY MADE CLOTHES IN SIZES XXS TO 6XL",
        "cta_text": "SHOP NOW",
        "header_bg": "#F5F5F5",
        "text_color": "#000000",
        "button_bg": "#000000",
        "button_text_color": "#FFFFFF",
        "bg_color": "#FFFFFF",
        "overlay_color": "#FFFFFF",
        "overlay_opacity": 15
      }
    }
  ]
}
{% endschema %}